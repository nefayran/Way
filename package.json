{
  "name": "way",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn build && yarn lint && lerna run start",
    "lint": "eslint --ext .js,.ts,.vue --ignore-path .gitignore",
    "test": "jest",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "jest": "^24.8.0",
    "lerna": "~3.14.0",
    "prettier": "^2.5.1"
  }
}
